class Main {
  public static void main(String[] args) {
    int[] arr = getRandomArray(10000);
    for(int i = 0; i < arr.length; i++){
      AVL.insertIter(arr[i]);
    }
    for(int i = 0; i < arr.length; i++){
      BST.insertIter(arr[i]);
    }
  }
}
