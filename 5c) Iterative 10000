class Main {
  public static void main(String[] args) {
    AVL tree = new AVL();
    int[] arr = getRandomArray(10000);
    for(int i = 0; i < arr.length; i++){
      AVL.tree.insertIter(tree.root, arr[i]);
    }
    for(int i = 0; i < arr.length; i++){
      BST.tree.insertIter(tree.root, arr[i]);
    }
  }
}
